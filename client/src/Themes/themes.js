import { createTheme } from '@mui/material/styles'


export const themes = createTheme({
  palette: {
    primary: {
      main: '#00587A',      // change primary for changing button colors
    },
    secondary: {
      main: '#0F3057', 
    },
  },
});

